{
  "installCommand": "npm install --include=dev --production=false",
  "buildCommand": "npm run build:web",
  "outputDirectory": "dist",
  "env": {
    "NPM_CONFIG_PRODUCTION": "false"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
